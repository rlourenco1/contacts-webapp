import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    header: {
        fontWeight: "bold",
    },
    btn: {
        // top, righ/left, bottom
        margin: "0 5px 0",
        cursor: "pointer"
    },
    // '.MuiTableCell-head':{
    //     fontWeight: "bold",
    // }
}));